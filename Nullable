using System;

namespace Nullable
{
    class MainApp
    {
        static void Main(string[] args)
        {
            int? a = null;
            
            Console.WriteLine(a.HasValue);
            Console.WriteLine(a != null);

            a = 3;

            Console.WriteLine(a.HasValue);
            Console.WriteLine(a != null);
            Console.WriteLine(a.Value);
        }
    }
}
// Nullable 형식의 이름은 Null(비어 있는) + able(~될 수 있는),  "비어있는 상태가 될 수 있" 형식이라는 뜻
